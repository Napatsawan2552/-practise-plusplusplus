#อันดับแรก สร้างinputเพื่อรับค่าตัวแปร a และ b
#สร้างพาร์ทการรับข้อมูลแบบบรรทัดต่อบรรทัด
a=input()
b=input()

#โดยปกติแล้วค่าตัวแปรที่input()รับมาคือค่าstringหรือค่าที่เป็นข้อความ แต่โจทย์ต้องการข้อมูลจำนวนเต็มหรือinteger เราจึงนำint()ครอบinput() เอาไว้เพื่อแปลงค่าstringข้อความเป็นintegerจำนวนเต็ม
#การใช้int()
a=int(input())
b=int(input())

#เมื่อรับข้อมูลจำนวนเต็มได้แล้ว โจทย์ต้องการให้หาผลรวมของ aและb หรือก็คือการนำไปบวกกันนั่นแหละ
#การนำตัวแปลaและbไปบวกกัน
a=int(input())
b=int(input())
sum = a + b

#สุดท้ายเราก็นำค่าsumที่ผ่านกระบวนการบวกนั้นแสดงผลออกทางหน้าจอหรือprint outค่าsumออกมา
a=int(input())
b=int(input())
sum = a + b
print(sum)

#จบแล้วกับการทำการบวกง่ายๆที่หลอนพอๆกับprint("Hello World!")เลย

#เพิ่มเติมคือสามารถเปลี่ยนเครื่องหมายเป็น -,*,**,/,//,%
